// @flow
import * as React from 'react';
import { Link } from 'react-router-dom';

const Join = (): React.Node => (
  <div>
    join
    <Link to="/login">
      login
    </Link>
  </div>
);

export default Join;
